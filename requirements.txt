# pyproject.toml is the single source of truth for all dependencies.
# This file exists for legacy workflows that expect requirements.txt.
# To install everything: pip install -r requirements.txt
# To install only test dependencies: pip install -e ".[test]"
-e .[all]
