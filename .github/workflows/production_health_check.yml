name: Production Health Check

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest

    env:
      TARGET_URL: https://kollipara-hema-ragcore.hf.space
      API_KEY: ${{ secrets.RAGCORE_API_KEY }}

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Run production health check
        run: python evaluation/scripts/production_health_check.py
